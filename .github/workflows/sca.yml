name: "Scheduled SCA"

on:
  workflow_dispatch:
  workflow_call:
    inputs:
      username:
        required: false
        type: string
    secrets:
      token:
        required: false
  schedule:
    - cron: '1 0 * * *'

jobs:
  sca:
    name: Scheduled SCA
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
                
      - name: SCA
        run: |
            dotnet tool restore
            dotnet run --project ./build/build.fsproj -t "SCA"

      - name: Run SCA
        uses: tonycknight/pkgchk-action@v1.0.13
